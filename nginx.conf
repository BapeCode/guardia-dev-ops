events {}

http {
    upstream glint {
        server backend:5000;
    }

    server {
        listen 8080;

        location / {
            proxy_pass http://glint;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}